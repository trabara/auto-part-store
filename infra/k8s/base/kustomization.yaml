apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - postgres.yaml
  - redis.yaml
  - minio.yaml
  - minio-init.yaml
  - medusa.yaml
  - medusa-init.yaml
  - storefront.yaml
  - ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: smap-store
    includeSelectors: true
