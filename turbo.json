{
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["build", "dist", ".medusa/server/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "start": {
      "dependsOn": ["^start"]
    },
    "dev": {
      "dependsOn": ["^build", "^dev:init"],
      "cache": false,
      "persistent": true
    },
    "dev:init": {
      "outputs": [".medusa/server/**"],
      "cache": false
    },
    "medusa:init": {
      "cache": false
    },
    "typecheck": {}
  }
}
